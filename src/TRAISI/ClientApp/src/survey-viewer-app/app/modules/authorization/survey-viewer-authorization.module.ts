import { NgModule } from "@angular/core";
import { AuthorizationService } from "./services/authorization.service";

@NgModule({
    declarations: [],
    providers: [AuthorizationService]
})
export class SurveyViewerAuthorizationModule { }
