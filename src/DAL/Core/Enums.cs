using System;

namespace DAL.Core
{
    public enum Gender
    {
        None,
        Female,
        Male
    }

    public enum SurveyViewType {
        RespondentView = 0,
        CatiView = 1
    }
}
